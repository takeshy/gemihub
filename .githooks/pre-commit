#!/bin/sh
# Run typecheck + lint before allowing a commit. Matches `npm run precommit`.
# Enable once per clone:  git config core.hooksPath .githooks
set -e
cd "$(git rev-parse --show-toplevel)"
npm run typecheck
npm run lint
